# ============================================
# Configuración del Add-in Evoex
# ============================================
# Copia este archivo como .env y reemplaza los valores con tus credenciales
# IMPORTANTE: Nunca subas el archivo .env a repositorios públicos

# ============================================
# Azure OpenAI Configuration
# ============================================

# Endpoint de Azure OpenAI
AZURE_OPENAI_ENDPOINT=https://tu-recurso.openai.azure.com

# API Key de Azure OpenAI
# Encuéntrala en: Azure Portal > Tu recurso OpenAI > Keys and Endpoint
AZURE_OPENAI_API_KEY=tu-api-key-aqui

# ============================================
# Proxy de Búsqueda Web (SearXNG Proxy)
# ============================================

# URL del proxy que maneja búsqueda web y RAG
SEARXNG_PROXY_URL=http://localhost:3001

# ============================================
# Modelos Disponibles (Configurable)
# ============================================

# Lista de modelos en formato JSON (opcional)
# Si no se define, usa los modelos por defecto
# Formato: [{"id":"modelo-id","name":"Nombre Visible","description":"Descripción"}]
AVAILABLE_MODELS=[{"id":"gpt-4o","name":"GPT-4o","description":"Modelo multimodal"}]

# Modelo predeterminado (opcional)
# Si no se define, usa el primer modelo de la lista
DEFAULT_MODEL_ID=gpt-4o
